name: Container bauen und veröffentlichen

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/container/**'
      - '.github/workflows/container.yml'

permissions:
  contents: read
  packages: write

concurrency:
  group: ghcr-container-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Repository auschecken
        uses: actions/checkout@v4

      - name: Docker Buildx einrichten
        uses: docker/setup-buildx-action@v4

      - name: Bei GHCR anmelden
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Container neu bauen und hochladen
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .github/container
          file: .github/container/Dockerfile
          platforms: linux/amd64
          pull: true
          no-cache: true
          push: true
          tags: |
            ghcr.io/philip222111/450-tictactest-mvk:latest
            ghcr.io/philip222111/450-tictactest-mvk:sha-${{ github.sha }}

      - name: Veröffentlichtes Image prüfen
        env:
          IMAGE_DIGEST: ${{ steps.build.outputs.digest }}
        run: |
          docker pull "ghcr.io/philip222111/450-tictactest-mvk@$IMAGE_DIGEST"
          docker run --rm "ghcr.io/philip222111/450-tictactest-mvk@$IMAGE_DIGEST" java -version
          echo "Veröffentlicht: ghcr.io/philip222111/450-tictactest-mvk@$IMAGE_DIGEST" >> "$GITHUB_STEP_SUMMARY"
